{
  "name": "simpeg-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"php -S 127.0.0.1:9012 -t api/public\" \"npm --prefix react run dev\"",
    "dev:api": "php -S 127.0.0.1:9012 -t api/public",
    "dev:web": "npm --prefix react run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}